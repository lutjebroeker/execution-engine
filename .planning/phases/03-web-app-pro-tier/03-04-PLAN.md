---
phase: 03-web-app-pro-tier
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - /root/projects/personal/flow-year-coach/.env.production
  - /root/projects/personal/flow-year-coach/vercel.json
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "App is live at a custom domain (not vercel.app subdomain)"
    - "Production build passes with zero console errors"
    - "All routes resolve correctly in the deployed environment (SPA routing works)"
    - "Supabase auth redirect URLs include the production domain"
    - "Environment variables (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_STRIPE_PUBLISHABLE_KEY) are set in Vercel"
    - "Stripe webhook points to production Supabase Edge Function URL"
  artifacts:
    - path: "/root/projects/personal/flow-year-coach/vercel.json"
      provides: "SPA rewrites already in place — verify and leave unchanged"
      contains: "rewrites"
    - path: "Vercel project (external)"
      provides: "flow-year-coach deployed at custom domain"
    - path: "Supabase Auth URL config (external)"
      provides: "Production domain added to allowed redirect URLs"
  key_links:
    - from: "Vercel deployment"
      to: "Supabase project gbmjsctlzothdbrlnzip"
      via: "VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY env vars"
      pattern: "VITE_SUPABASE_URL"
    - from: "Stripe production webhook"
      to: "supabase/functions/stripe-webhook"
      via: "https://gbmjsctlzothdbrlnzip.supabase.co/functions/v1/stripe-webhook"
      pattern: "stripe-webhook"

user_setup:
  - service: vercel
    why: "Deployment to Vercel requires CLI login and GitHub repo connection"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard → Settings → API → Project URL"
        value: "https://gbmjsctlzothdbrlnzip.supabase.co"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Settings → API → anon public key"
      - name: VITE_STRIPE_PUBLISHABLE_KEY
        source: "Stripe Dashboard → Developers → API keys → Publishable key (use live key for production)"
    dashboard_config:
      - task: "Connect GitHub repo to Vercel"
        location: "vercel.com → New Project → Import lutjebroeker/flow-year-coach"
        note: "Set framework preset to Vite. Root directory: / (or flow-year-coach if needed)"
      - task: "Set custom domain"
        location: "Vercel project → Settings → Domains → Add domain"
        note: "Update DNS CNAME record at your registrar to point to cname.vercel-dns.com"
  - service: supabase
    why: "Supabase Auth redirect URLs must include production domain"
    dashboard_config:
      - task: "Add production domain to allowed redirect URLs"
        location: "Supabase Dashboard → Authentication → URL Configuration → Redirect URLs"
        note: "Add: https://yourdomain.com, https://yourdomain.com/update-password, https://yourdomain.com/"
---

<objective>
Deploy the completed flow-year-coach app to Vercel with a custom domain. Verify production build, confirm all routes work, and ensure env vars and auth redirects are correctly configured for production.

Purpose: Without deployment, the Pro tier product doesn't exist for customers. This is the ship step.
Output: Live app at custom domain with working auth, payments, and AI features.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/root/projects/personal/execution-engine/.planning/ROADMAP.md
@/root/projects/personal/execution-engine/.planning/phases/03-web-app-pro-tier/03-01-SUMMARY.md
@/root/projects/personal/execution-engine/.planning/phases/03-web-app-pro-tier/03-02-SUMMARY.md
@/root/projects/personal/execution-engine/.planning/phases/03-web-app-pro-tier/03-03-SUMMARY.md

<interfaces>
<!-- vercel.json already exists with SPA rewrites -->
From /root/projects/personal/flow-year-coach/vercel.json:
- Already has rewrites: [{ "source": "/(.*)", "destination": "/index.html" }] for SPA routing
- No changes needed to vercel.json unless build command or output dir is wrong

Supabase project: gbmjsctlzothdbrlnzip (eu-west-1)
GitHub: lutjebroeker/flow-year-coach
Build command: npm run build
Output dir: dist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy verification + production build check</name>
  <files>
    /root/projects/personal/flow-year-coach/vercel.json
  </files>
  <action>
    **Step 1 — Run production build locally:**
    ```bash
    cd /root/projects/personal/flow-year-coach
    npm run build
    ```
    Build must pass with zero errors. If there are TypeScript errors or build failures, fix them before proceeding. Common issues to check:
    - Unused imports added in plans 03-01 through 03-03
    - Missing type exports (check all new hooks and contexts)
    - Environment variable references without VITE_ prefix being bundled

    **Step 2 — Verify vercel.json:**
    Read /root/projects/personal/flow-year-coach/vercel.json. It should already contain SPA rewrites. If the file is correct, leave it unchanged. If it is missing or malformed, write the correct content:
    ```json
    {
      "rewrites": [
        { "source": "/(.*)", "destination": "/index.html" }
      ]
    }
    ```

    **Step 3 — Print pre-deploy checklist for human:**
    Output the following checklist to console so the human can work through it:

    ```
    PRE-DEPLOY CHECKLIST — verify each before deploying:

    Vercel:
    [ ] vercel.json has SPA rewrites (already done)
    [ ] npm run build passes locally (Task 1 above)
    [ ] Vercel project linked to GitHub repo lutjebroeker/flow-year-coach
    [ ] Env vars set in Vercel dashboard:
        - VITE_SUPABASE_URL = https://gbmjsctlzothdbrlnzip.supabase.co
        - VITE_SUPABASE_ANON_KEY = (from Supabase dashboard)
        - VITE_STRIPE_PUBLISHABLE_KEY = pk_live_... (production key)

    Supabase:
    [ ] Production domain added to Supabase Auth → URL Configuration → Redirect URLs
    [ ] supabase/functions/* deployed (from plan 03-02)
    [ ] Supabase Edge Function secrets set: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID, ANTHROPIC_API_KEY

    Stripe:
    [ ] Production webhook endpoint registered pointing to:
        https://gbmjsctlzothdbrlnzip.supabase.co/functions/v1/stripe-webhook
    [ ] Production Stripe keys (not test keys) used in production

    Deploy command:
    cd /root/projects/personal/flow-year-coach && npx vercel --prod
    ```

    **Step 4 — Verify build output:**
    After `npm run build`, check dist/ exists and contains index.html:
    `ls /root/projects/personal/flow-year-coach/dist/`

    If build fails, fix the errors. Common fixes:
    - If Stripe import fails: verify @stripe/stripe-js is in package.json
    - If supabase type errors: check that new tables were added to Database type in supabase.ts
    - If context import errors: verify SubscriptionContext.tsx exports SubscriptionProvider and useSubscription
  </action>
  <verify>
    `cd /root/projects/personal/flow-year-coach && npm run build && echo "BUILD OK" && ls dist/index.html`
    Must output "BUILD OK" and confirm dist/index.html exists.
  </verify>
  <done>
    npm run build passes locally. dist/ directory contains index.html and hashed JS/CSS assets. vercel.json is correct. Pre-deploy checklist printed to terminal.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Production build verified locally. Pre-deploy checklist provided. Ready for Vercel deployment and custom domain setup.
  </what-built>
  <how-to-verify>
    Work through the pre-deploy checklist printed by Task 1, then:

    1. Set environment variables in Vercel dashboard (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_STRIPE_PUBLISHABLE_KEY)
    2. Deploy: `cd /root/projects/personal/flow-year-coach && npx vercel --prod`
    3. Visit the deployed URL (Vercel provides it after deploy)
    4. Test each route works (no 404 on refresh):
       - /login → renders Login page with 3 tabs
       - /vision → renders Vision page (auth-gated — redirects to /login if not signed in)
       - /planner → renders Planner page
       - /update-password → renders UpdatePassword page
    5. Sign in → verify auth works in production (magic link email arrives)
    6. Go to /instellingen → verify subscription card shows (Free or Pro)
    7. Check browser console: zero console.error calls
    8. Add custom domain in Vercel project → Domains → add domain
    9. Update DNS CNAME at registrar to point to cname.vercel-dns.com
    10. Once DNS propagates: verify app loads at custom domain
    11. Test Stripe checkout flow end-to-end with production keys (small real payment or use Stripe test mode temporarily)
  </how-to-verify>
  <resume-signal>Type "approved" with the live URL, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- npm run build passes with zero errors
- Deployed URL loads the app
- All routes resolve correctly (no 404 on direct URL access)
- No console.error in browser devtools after login
- Auth works: magic link email arrives and signs user in
- Custom domain resolves (DNS may take up to 24 hours)
- Supabase Auth redirect URLs include the production domain
</verification>

<success_criteria>
- DEPLOY-01: App deployed to Vercel (npx vercel --prod) — accessible at custom domain
- DEPLOY-02: Environment variables set in Vercel: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_STRIPE_PUBLISHABLE_KEY. Supabase Edge Function secrets: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_ID, ANTHROPIC_API_KEY
- DEPLOY-03: npm run build passes locally and in Vercel CI — no console errors in production, all routes work via SPA rewrites
</success_criteria>

<output>
After completion, create `/root/projects/personal/execution-engine/.planning/phases/03-web-app-pro-tier/03-04-SUMMARY.md`
</output>
