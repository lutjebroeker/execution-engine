---
phase: 01-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - style.css
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - COPY-04
  - COPY-05
  - COPY-06
  - COPY-07
  - COPY-08
  - COPY-09
  - COPY-10
  - COPY-11
  - DMND-04
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04

must_haves:
  truths:
    - "Visitor reads the hero headline and immediately understands who this is for and what problem it solves"
    - "All 13 page sections render in the correct Hormozi/Priestley order with real copy — no Lorem Ipsum, no placeholder text"
    - "Both email capture forms exist in the DOM with correct class names (.waitlist-form) ready for JS wiring"
    - "Page is fully mobile-responsive — no horizontal scroll, legible text, working layout at 375px viewport"
    - "Demo section shows Telegram bot interaction (static HTML mockup if no video asset yet)"
  artifacts:
    - path: "index.html"
      provides: "Complete single-page HTML with all 13 sections, OG tag shells, correct form markup"
      contains: "class=\"waitlist-form\""
    - path: "style.css"
      provides: "All CSS — BRAND.md custom properties, mobile-first responsive layout, all section styles"
      contains: "--blue-brand: #1D4ED8"
  key_links:
    - from: "index.html form.waitlist-form"
      to: "main.js handleWaitlistSubmit"
      via: "class selector — querySelectorAll('.waitlist-form')"
      pattern: "class=\"waitlist-form\""
    - from: "style.css :root"
      to: "BRAND.md color palette"
      via: "CSS custom properties matching BRAND.md exactly"
      pattern: "--blue-brand.*#1D4ED8"
---

<objective>
Build the complete HTML structure and CSS for the Execution Engine landing page — all 13 sections with real copy, full mobile-responsive styling using BRAND.md design tokens, and form markup ready for JavaScript wiring in Plan 01-02.

Purpose: This plan produces the complete visible page. Plan 01-02 only adds JS behaviour and deployment. A reviewer reading this plan's output should see a fully styled, copy-complete landing page when opened in a browser (minus form submit functionality and analytics).

Output:
- index.html — 13-section landing page with complete copy, OG tag shells, correct form classes
- style.css — full CSS, BRAND.md variables, mobile-first responsive
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-landing-page/01-CONTEXT.md
@.planning/phases/01-landing-page/01-RESEARCH.md
@BRAND.md

<interfaces>
<!-- CSS custom properties from BRAND.md — use verbatim, do not rename -->
:root {
  --blue-brand:       #1D4ED8;
  --blue-light:       #3B82F6;
  --blue-pale:        #DBEAFE;
  --blue-muted:       #EEF2FF;
  --bg-white:         #FFFFFF;
  --bg-off-white:     #F5F7FA;
  --bg-paper:         #F5F1EB;
  --bg-paper-dark:    #EDE8E1;
  --gray-border:      #E5E7EB;
  --gray-light:       #F3F4F6;
  --gray-mid:         #9CA3AF;
  --gray-text:        #6B7280;
  --gray-dark:        #374151;
  --gray-heading:     #1a1a2e;
  --green-success:    #34D399;
  --amber-warning:    #F59E0B;
  --red-soft:         #F87171;
  --max-prose:        680px;
  --max-ui:           1200px;
  --max-narrow:       480px;
  --radius-sm:        4px;
  --radius-md:        8px;
  --radius-lg:        12px;
  --radius-xl:        16px;
  --shadow-card:      0 4px 16px rgba(0,0,0,0.06), 0 1px 4px rgba(0,0,0,0.04);
  --text-xs:          0.75rem;
  --text-sm:          0.875rem;
  --text-base:        1rem;
  --text-lg:          1.125rem;
  --text-xl:          1.25rem;
  --text-2xl:         1.5rem;
  --text-3xl:         1.875rem;
  --text-4xl:         2.25rem;
  --text-5xl:         3rem;
}

<!-- Form class contract — Plan 01-02 wires JS using this selector -->
<!-- BOTH forms must use: class="waitlist-form" -->
<!-- Input must be: type="email" name="email" -->
<!-- Button must be: type="submit" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write index.html — complete 13-section landing page</name>
  <files>index.html</files>
  <action>
Create a single `index.html` file at the repo root. This is the complete landing page — no build step, self-contained.

**Document shell (exact):**
- `<!DOCTYPE html>`, `lang="en"`, `charset="UTF-8"`
- `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- `<title>Execution Engine — Build systems that actually execute.</title>`
- OG tag shells with placeholder domain (use `https://executionengine.vercel.app` for now — Plan 01-02 updates after deploy):
  - `og:type="website"`, `og:title`, `og:description`, `og:image`, `og:image:width="1200"`, `og:image:height="630"`, `og:url`
  - `twitter:card="summary_large_image"`, `twitter:title`, `twitter:description`, `twitter:image`
- `<link rel="stylesheet" href="style.css">`
- Script tags at end of body (empty placeholders that Plan 01-02 fills): Supabase CDN `defer`, `main.js` `defer`, Plausible `defer`

**Section order — implement ALL 13 in this exact sequence:**

1. **Nav** — `<nav>`: logo text "Execution Engine" left-aligned, "Join Waitlist" anchor right-aligned pointing to `#waitlist`. No other nav items. Minimal, sticky on scroll optional.

2. **Hero** (`id="hero"`) — Georgia headline: "You have a system. You just don't follow through." Subheadline (System UI body): "Execution Engine is the working system — Telegram AI check-ins, 12-week cycles, confrontational accountability. Built by a founder. Running in production." Proof stat: "23% → 87% Execution Rate in 12 weeks." Email form (`class="waitlist-form"`, `id="hero-form"`): one email input + "Join Waitlist" submit button. Post-submit confirmation slot: `<div class="form-success" hidden>You're on the list. Watch your inbox.</div>`

3. **Problem** (`id="problem"`) — Section heading: "You've tried the systems. They didn't stick." Body: 3-4 short paragraphs naming the specific pain — Notion templates abandoned, productivity courses never finished, morning routines that lasted 2 weeks. Tone: calm, direct, no blame. "Life gets busy. The system breaks. And six months later you're starting over."

4. **Agitation** — No `id` needed. Heading: "The cost isn't the failed system. It's the years." Body: name the real cost — projects half-done, goals carried forward for the 4th year, the feeling of capable-but-not-executing. Use BRAND.md energy: "You said you'd do this." Direct, confrontational, not harsh.

5. **Solution** (`id="solution"`) — Heading: "Execution Engine is not a course. It's the car — already running." Body: explain that this is a working system, not instructions. Real n8n workflows (15+), real Telegram bot checking in daily, real Obsidian vault tracking cycles. Competitive line: "LifeOS tells you how to drive. Execution Engine is the car — already running." (per CONTEXT.md)

6. **How it works** (`id="how-it-works"`) — Heading: "How it works." Five numbered steps rendered as a vertical list or horizontal flow (use CSS grid on desktop, stacked on mobile):
   - Step 1: Vision — Set your 12-week vision and 3 goals
   - Step 2: 12-Week Cycle — Break goals into weekly actions
   - Step 3: Daily AI Check-in — Telegram bot asks "Did you do the work?" every morning
   - Step 4: Weekly Review — Rate your week, adjust actions
   - Step 5: Quarterly Review — Reflect, reset, repeat

7. **Demo** (`id="demo"`) — DMND-04. Heading: "See it in action." If no video asset exists yet (likely), render a styled static HTML mockup of a Telegram conversation: a `<div class="telegram-mockup">` showing 3-4 realistic bot message bubbles. Example messages: "Good morning. Yesterday you planned to work on your landing page copy. Did you do it? (y/n)", then a user "y", then bot "87% this week. Keep the streak." Style as mobile phone frame with dark background and white/green bubbles. Add a caption: "Your AI check-in, every morning on Telegram." Mark with HTML comment: `<!-- DMND-04: replace with <video> when demo.mp4 is ready -->`

8. **Social proof** (`id="social-proof"`) — Heading: "I built this for myself. Here's what happened." First-person framing by Jelle. Show 3 stat cards: "23% → 87%" (Execution Rate), "12 Completed Cycles", "15+ Active Workflows". Below stats: a short paragraph — "n=1 transparency: I'm selling you the car I drive. These are my numbers." Use BRAND.md card style with left accent bar.

9. **Value stack** (`id="value-stack"`) — Heading: "What you get." Hormozi-style HTML table (`class="value-stack"`):
   - n8n Morning System (15+ workflows) — ~~€297~~
   - Obsidian Vault Starter — ~~€97~~
   - Setup Guide + Quick Start — ~~€147~~
   - 1:1 Setup Call (first 20 buyers only) — ~~€249~~
   - **Total perceived value: €652+**
   - **Launch price: €2,497** (styled prominently)
   - Below table: "Full price after launch: €4,997. Join the waitlist to lock in launch pricing."

10. **Guarantee** (`id="guarantee"`) — Heading: "Day 1 result or full refund." Body: "Set up your vault, complete your first Telegram check-in. If that doesn't work on Day 1, you get every euro back. No questions." Warm paper background (`--bg-paper`).

11. **Scarcity** (`id="scarcity"`) — Heading: "First 20 buyers get a free 1:1 setup call." Body: "90 minutes. We set up your vault, connect your Telegram bot, configure your first 12-week cycle. Together. €249 value, free for the first 20." Add a visual counter placeholder: `<span class="spots-remaining">20 spots available</span>` (static for now).

12. **FAQ** (`id="faq"`) — Heading: "Questions." Four mandated Q&A pairs using `<details>`/`<summary>` HTML (no JS accordion needed):
    - Q: "Do I need to know n8n?" A: "No. The workflows are pre-built. You import one JSON file and configure three settings. That's it."
    - Q: "Do I need Obsidian?" A: "Obsidian is part of the system. If you've never used it, the vault starter walks you through setup from scratch. It's free software."
    - Q: "What if I'm not technical?" A: "You need to be comfortable copying and pasting into a config file. That's the technical bar. If you've ever set up a Notion database, you can set this up."
    - Q: "How is this different from LifeOS?" A: "LifeOS tells you how to drive. Execution Engine is the car — already running. LifeOS is a Notion template. This is an automated system that checks in on you every morning whether you like it or not."

13. **Final CTA** (`id="waitlist"`) — Heading: "Join the waitlist." Subheading: "Get launch pricing when we ship. First 20 buyers get the 1:1 setup call." Second email form (`class="waitlist-form"`, `id="cta-form"`): same structure as hero form. Below: repeat guarantee line — "Day 1 result or full refund."

**Footer:** tagline "Build systems that actually execute." and "© 2026 Execution Engine · Jelle Spek"

**Critical constraints:**
- NO placeholder copy — all sections get real copy as specified above
- NO fake testimonials from other people — only Jelle's personal stats
- NO mention of €149 — price is €2,497 launch / €4,997 full (BRAND.md authoritative)
- NO separate thank-you page — inline confirmation only
- Georgia for all `h1`, `h2`, `h3` — System UI for all body text
- Both forms MUST have `class="waitlist-form"` (JS in Plan 01-02 selects via this class)
  </action>
  <verify>
    <automated>
      grep -c 'class="waitlist-form"' index.html | grep -q "^2$" && echo "PASS: 2 forms" || echo "FAIL: wrong form count"
      grep -q 'id="hero"' index.html && grep -q 'id="problem"' index.html && grep -q 'id="faq"' index.html && echo "PASS: key sections present" || echo "FAIL: missing sections"
      grep -q 'og:image' index.html && echo "PASS: OG tags present" || echo "FAIL: missing OG tags"
    </automated>
  </verify>
  <done>index.html contains all 13 sections in correct order, both forms with class="waitlist-form", OG tag shells, real copy throughout (no Lorem Ipsum), Georgia fonts on headings, Telegram mockup in demo section, value stack table with correct prices (€2,497 launch / €4,997 full)</done>
</task>

<task type="auto">
  <name>Task 2: Write style.css — mobile-first responsive stylesheet</name>
  <files>style.css</files>
  <action>
Create `style.css` at repo root. All BRAND.md design tokens as CSS custom properties. Mobile-first: base styles target 375px, `@media (min-width: 768px)` for tablet, `@media (min-width: 1024px)` for desktop.

**CSS structure (in this order):**

1. **`:root`** — paste ALL BRAND.md custom properties verbatim (colors, type scale, spacing, radius, shadows, max-widths)

2. **Reset/base** — `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`, body: `font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; color: var(--gray-dark); background: var(--bg-white); line-height: 1.6;`

3. **Typography** — `h1, h2, h3 { font-family: Georgia, 'Times New Roman', serif; font-weight: 700; letter-spacing: -0.02em; color: var(--gray-heading); }` — DO NOT add any Google Fonts link for Georgia (system font). Type scale classes: `.text-5xl { font-size: var(--text-5xl); }` etc for each scale step.

4. **Layout utilities** — `.container { max-width: var(--max-ui); margin: 0 auto; padding: 0 1.25rem; }`, `.prose { max-width: var(--max-prose); }`, `.section { padding: 3rem 1.25rem; }` with `@media (min-width: 768px) { .section { padding: 5rem 2rem; } }`

5. **Section backgrounds** — alternate white / off-white / paper:
   - `.section--white { background: var(--bg-white); }`
   - `.section--off-white { background: var(--bg-off-white); }`
   - `.section--paper { background: var(--bg-paper); }`

6. **Nav** — fixed/sticky top, white background, `box-shadow: 0 1px 0 var(--gray-border)`, flex space-between, logo bold Georgia, CTA link styled as text link in `--blue-brand`

7. **Hero** — large heading `font-size: var(--text-4xl)` on mobile, `var(--text-5xl)` at 768px+. Stat badge: `background: var(--blue-pale); color: var(--blue-brand); border-radius: var(--radius-pill); padding: 0.25rem 1rem; font-weight: 600;` displaying "23% → 87%"

8. **Forms** — `.waitlist-form { display: flex; flex-direction: column; gap: 0.75rem; max-width: var(--max-narrow); }` at mobile, `flex-direction: row` at 640px+. Email input: full width, border `1px solid var(--gray-border)`, radius `var(--radius-md)`, padding `0.75rem 1rem`. Submit button: BRAND.md primary button — `background: var(--blue-brand); color: white; border: none; border-radius: var(--radius-md); padding: 0.75rem 1.5rem; font-weight: 600; cursor: pointer; white-space: nowrap;`. Hover: `background: var(--blue-light)`. Disabled state: `opacity: 0.6; cursor: not-allowed;`

9. **Telegram mockup** — `.telegram-mockup { background: #1c2733; border-radius: var(--radius-xl); padding: 1.5rem; max-width: 360px; margin: 2rem auto; }` — bot bubbles: `background: #2b5278; color: white; border-radius: 12px 12px 12px 4px; padding: 0.75rem 1rem; margin-bottom: 0.75rem;` — user bubbles: `background: var(--blue-brand); color: white; border-radius: 12px 12px 4px 12px; text-align: right;`

10. **Stat cards** — `.stat-card { background: var(--bg-white); border: 1px solid var(--gray-border); border-left: 4px solid var(--blue-brand); border-radius: var(--radius-lg); padding: 1.5rem; box-shadow: var(--shadow-card); }` — `.stat-value { font-family: Georgia, serif; font-size: var(--text-4xl); color: var(--blue-brand); font-weight: 700; }`. Cards grid: `.stats-grid { display: grid; grid-template-columns: 1fr; gap: 1rem; }` at mobile, `grid-template-columns: repeat(3, 1fr)` at 768px+

11. **Value stack table** — `.value-stack { width: 100%; border-collapse: collapse; }` — rows: alternating `var(--bg-white)` and `var(--gray-light)` — `.value-stack__total td { font-weight: 700; border-top: 2px solid var(--gray-border); }` — `.value-stack__price td { font-size: var(--text-2xl); font-weight: 700; color: var(--blue-brand); font-family: Georgia, serif; }` — `s { color: var(--gray-mid); text-decoration: line-through; }`

12. **How it works steps** — `.steps-list { list-style: none; counter-reset: step; display: grid; grid-template-columns: 1fr; gap: 1.5rem; }` at mobile, `grid-template-columns: repeat(5, 1fr)` at 1024px+. Each step: counter increment, number badge in `--blue-brand`.

13. **FAQ accordion** — `details { border-bottom: 1px solid var(--gray-border); padding: 1rem 0; }` — `summary { font-weight: 600; cursor: pointer; list-style: none; display: flex; justify-content: space-between; }` — `summary::after { content: '+'; color: var(--blue-brand); }` — `details[open] summary::after { content: '−'; }` — `details p { margin-top: 1rem; color: var(--gray-dark); }`

14. **Form success/error states** — `.form-success { color: var(--gray-dark); font-weight: 600; padding: 1rem; background: var(--blue-muted); border-radius: var(--radius-md); }` — `.form-error { color: #991B1B; font-size: var(--text-sm); margin-top: 0.5rem; }`

15. **Scarcity/guarantee sections** — guarantee uses `--bg-paper` background with large Georgia heading. Scarcity: `.spots-remaining { display: inline-block; background: var(--amber-warning); color: #1a1a2e; border-radius: var(--radius-pill); padding: 0.25rem 1rem; font-weight: 700; font-size: var(--text-sm); }`

16. **Footer** — centered, `--bg-off-white` background, `--gray-text` color, tagline in Georgia italic

**Critical constraints:**
- NO `@import` for Google Fonts (use `<link>` in HTML if needed — but Georgia is system font, no CDN)
- NO external CSS framework — pure vanilla CSS only
- Mobile-first: base styles are for 375px, breakpoints expand upward
- All colors via CSS custom properties — no hardcoded hex values except in `:root`
  </action>
  <verify>
    <automated>
      grep -q '\-\-blue-brand:.*#1D4ED8' style.css && echo "PASS: brand color present" || echo "FAIL: missing brand color"
      grep -q '@media (min-width: 768px)' style.css && echo "PASS: responsive breakpoints present" || echo "FAIL: missing breakpoints"
      grep -q 'Georgia' style.css && echo "PASS: Georgia font present" || echo "FAIL: missing Georgia"
      grep -q '@import' style.css && echo "FAIL: @import found (anti-pattern)" || echo "PASS: no @import"
    </automated>
  </verify>
  <done>style.css has all BRAND.md custom properties in :root, mobile-first responsive layout, Georgia headlines, System UI body, all section/component styles for every section in index.html. Opening index.html in a browser shows a styled, professional-looking page at both 375px and 1440px viewport widths.</done>
</task>

</tasks>

<verification>
Open index.html directly in a browser (no server needed for this plan — no JS runs yet):
1. All 13 sections visible, in correct Priestley/Hormozi order
2. No Lorem Ipsum or "[placeholder]" text anywhere
3. Hero headline reads "You have a system. You just don't follow through."
4. Value stack shows €2,497 launch price (NOT €149)
5. Both email forms render (no submit functionality yet — that's Plan 01-02)
6. Telegram mockup shows realistic bot conversation bubbles
7. Resize to 375px — no horizontal scroll, all text legible
8. View source: OG tag shells present in `<head>`
</verification>

<success_criteria>
- index.html: complete 13-section page, real copy throughout, both forms with class="waitlist-form", OG shells
- style.css: BRAND.md variables in :root, mobile-first responsive, no external CSS dependencies
- Visual: page renders correctly in browser at mobile (375px) and desktop (1440px) without JS
- Price: €2,497 launch / €4,997 full displayed correctly in value stack
- Fonts: Georgia on all headings, System UI on body — no Google Fonts for Georgia
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page/01-01-SUMMARY.md` with:
- Files created and their line counts
- Key copy decisions made (hero headline, section copy tone)
- Any deviations from the plan (e.g., if Telegram mockup used different approach)
- CSS patterns established (for Plan 01-02 reference)
- State of DMND-04 (mockup vs real video)
</output>
