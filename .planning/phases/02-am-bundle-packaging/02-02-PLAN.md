---
phase: 02-am-bundle-packaging
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - bundle/README.md
  - bundle/obsidian-vault-starter/Daily/_template-daily.md
  - bundle/obsidian-vault-starter/Daily/example-2026-03-01.md
  - bundle/obsidian-vault-starter/Weekly/_template-weekly.md
  - bundle/obsidian-vault-starter/Weekly/example-2026-W09.md
  - bundle/obsidian-vault-starter/Quarterly/_template-quarterly.md
  - bundle/obsidian-vault-starter/Quarterly/example-2026-Q1.md
  - bundle/obsidian-vault-starter/Goals/_template-goal.md
  - bundle/obsidian-vault-starter/Archive/.gitkeep
  - bundle/obsidian-vault-starter/.obsidian/community-plugins.json
  - bundle/obsidian-vault-starter/.obsidian/appearance.json
  - bundle/execution-engine-am-bundle.zip
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - DOCS-06
  - BUNDLE-02
  - BUNDLE-03

must_haves:
  truths:
    - "A complete beginner can follow the README from prerequisites checklist to first Telegram check-in without guessing any step"
    - "The Obsidian Vault Starter ZIP unpacks to a folder structure that exactly matches what the n8n workflows read and write"
    - "Every tool section in the README ends with a verification step the buyer can run to confirm that tool is working before moving to the next"
    - "The Vault Starter includes the .obsidian/ config folder with Local REST API pre-configured in community-plugins.json"
    - "The final ZIP contains README.md, workflows/am-workflows-v1.json, and the full obsidian-vault-starter/ tree"
  artifacts:
    - path: "bundle/README.md"
      provides: "Master setup guide — prerequisites, numbered setup steps for all 5 tools, workflow reference grouped by routine"
      min_lines: 200
    - path: "bundle/obsidian-vault-starter/"
      provides: "Pre-built vault folder structure: Daily/Weekly/Quarterly/Goals/Archive + .obsidian/ config"
    - path: "bundle/execution-engine-am-bundle.zip"
      provides: "Shippable bundle ZIP containing all three deliverables"
  key_links:
    - from: "bundle/obsidian-vault-starter/Daily/"
      to: "n8n workflow Obsidian API calls"
      via: "vaultRootPath in Config node + folder name"
      pattern: "Daily/ folder matches workflow path references"
    - from: "bundle/README.md workflow reference section"
      to: "bundle/audit-notes.md workflow list"
      via: "DOCS-06 — workflow names and descriptions sourced from audit"
      pattern: "Each workflow listed under correct routine group"
---

<objective>
Write the master README setup guide and create the Obsidian Vault Starter ZIP, then package everything (README + workflows JSON + vault starter) into the final shippable bundle ZIP.

Purpose: A buyer who has never used Obsidian, Telegram bots, Ollama, or PostgreSQL must be able to follow this guide from zero to first Telegram check-in in 30 minutes.
Output: `bundle/README.md` (complete setup guide), `bundle/obsidian-vault-starter/` (pre-built vault), `bundle/execution-engine-am-bundle.zip` (shippable bundle).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-am-bundle-packaging/02-CONTEXT.md
@.planning/phases/02-am-bundle-packaging/02-RESEARCH.md
@.planning/phases/02-am-bundle-packaging/02-01-SUMMARY.md

<interfaces>
<!-- README structure (locked by CONTEXT.md) -->
<!--
# Execution Engine AM Bundle — Setup Guide

## Prerequisites Checklist
- [ ] n8n installed and running (link to official docs)
- [ ] Telegram account (any device)
- [ ] Obsidian installed (link)
- [ ] Ollama installed with [model from audit-notes] downloaded (link)
- [ ] PostgreSQL running (link)
[+ any community node install if audit found one]

## Step 1: Set Up Telegram Bot
[numbered steps with exact copy-paste values]
Verification: Send /start to your bot — it should reply "Hello"

## Step 2: Set Up Obsidian Vault
[numbered steps — extract vault starter, note the path]
Verification: [specific check]

## Step 3: Set Up Obsidian Local REST API Plugin
[numbered steps — plugin list, enable, generate API key, note the key]
Verification: [specific check]

## Step 4: Set Up Ollama
[numbered steps — model pull command from audit-notes]
Verification: curl http://localhost:11434/api/tags returns the model name

## Step 5: Set Up PostgreSQL
[numbered steps — CREATE TABLE statements from audit-notes postgres schema]
Verification: psql command that confirms tables exist

## Step 6: Import Workflows into n8n
[numbered steps — import JSON, note inactive state, attach credentials]
Verification: All workflows appear in workflow list

## Step 7: Configure the Config Node
[numbered steps — open each workflow, find "CONFIG — Edit These Values", fill in values collected in steps 1-6]
Verification: Run the Morning Routine workflow manually — it should complete without errors

## Workflow Reference
### Morning Routine
- [workflow name]: [description]
### Evening Routine
...
### Week Planning
...
### Quarter Review
...
### Message Hub
...

## Support
Email: support@jellespek.nl
Response time: within 24 hours
-->

<!-- Vault folder structure (locked by CONTEXT.md) -->
<!--
obsidian-vault-starter/
├── Daily/
│   ├── _template-daily.md
│   └── example-2026-03-01.md
├── Weekly/
│   ├── _template-weekly.md
│   └── example-2026-W09.md
├── Quarterly/
│   ├── _template-quarterly.md
│   └── example-2026-Q1.md
├── Goals/
│   └── _template-goal.md
├── Archive/
│   └── .gitkeep
└── .obsidian/
    ├── community-plugins.json
    └── appearance.json
-->

<!-- Daily note frontmatter template (from RESEARCH.md) -->
<!--
---
date: {{date:YYYY-MM-DD}}
type: daily
week: {{date:YYYY-[W]WW}}
quarter: {{date:YYYY-[Q]Q}}
mood:
energy:
focus_score:
---

## Morning Intent
_What matters most today?_

## Tasks
- [ ]

## Evening Review
_What got done? What blocked progress?_

## Tomorrow
-
-->

<!-- .obsidian/community-plugins.json (from RESEARCH.md) -->
<!--
{
  "plugins": ["obsidian-local-rest-api"],
  "enabled": ["obsidian-local-rest-api"]
}
Note: actual Obsidian plugin dir structure needs core-plugins.json too
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Obsidian Vault Starter folder structure and template files</name>
  <files>
    bundle/obsidian-vault-starter/Daily/_template-daily.md,
    bundle/obsidian-vault-starter/Daily/example-2026-03-01.md,
    bundle/obsidian-vault-starter/Weekly/_template-weekly.md,
    bundle/obsidian-vault-starter/Weekly/example-2026-W09.md,
    bundle/obsidian-vault-starter/Quarterly/_template-quarterly.md,
    bundle/obsidian-vault-starter/Quarterly/example-2026-Q1.md,
    bundle/obsidian-vault-starter/Goals/_template-goal.md,
    bundle/obsidian-vault-starter/Archive/.gitkeep,
    bundle/obsidian-vault-starter/.obsidian/community-plugins.json,
    bundle/obsidian-vault-starter/.obsidian/appearance.json
  </files>
  <action>
    Read `bundle/audit-notes.md` first. Confirm the workflow routines use the folder names Daily/Weekly/Quarterly/Goals/Archive. If the audit notes show the live system uses different folder names, use those exact names instead.

    Create the complete vault starter directory structure. All example notes use fictional but realistic English placeholders — no personal data from the live system.

    **Daily/_template-daily.md:**
    Use frontmatter from the interfaces block. Body: "## Morning Intent", "## Tasks" with `- [ ]`, "## Evening Review", "## Tomorrow" sections.

    **Daily/example-2026-03-01.md:**
    Filled-in example daily note with fictional data. Morning Intent: "Complete the project proposal first draft". Tasks: "- [x] Review Q1 goals", "- [ ] Write project proposal". Evening Review: "Finished proposal draft, blocked on budget numbers". Tomorrow: "Request budget numbers from finance team".

    **Weekly/_template-weekly.md:**
    Frontmatter: `week`, `type: weekly`, `start_date`, `end_date`, `execution_score`. Body sections: "## Week Intent", "## Goals This Week", "## Daily Wins", "## Blockers", "## Next Week".

    **Weekly/example-2026-W09.md:**
    Fictional filled example. Execution score: 74. Goals: "Finish project proposal", "Review Q1 cycle". Wins: "Delivered proposal on Tuesday". Blockers: "Budget approval pending".

    **Quarterly/_template-quarterly.md:**
    Frontmatter: `quarter` (e.g. `2026-Q1`), `type: quarterly`, `execution_score`. Body: "## Quarter Theme", "## 12-Week Goals", "## Results", "## Lessons", "## Next Quarter".

    **Quarterly/example-2026-Q1.md:**
    Fictional example. Theme: "Build the foundation". Goals: "Launch first digital product", "Establish daily review habit". Results: "Product launched week 10", "Daily review streak: 68 days".

    **Goals/_template-goal.md:**
    Frontmatter: `goal`, `target_date`, `status: active | complete | dropped`, `12_week_cycle`. Body: "## Why This Matters", "## Success Looks Like", "## Weekly Actions", "## Progress Notes".

    **Archive/.gitkeep:** empty file.

    **.obsidian/community-plugins.json:**
    ```json
    {
      "plugins": ["obsidian-local-rest-api"],
      "enabled": ["obsidian-local-rest-api"]
    }
    ```

    **.obsidian/appearance.json:**
    Minimal light theme config:
    ```json
    {
      "theme": "",
      "cssTheme": "",
      "baseFontSize": 16,
      "enabledCssSnippets": []
    }
    ```

    Note: The buyer still needs to install the Local REST API plugin from Obsidian community plugins and enable it — the `.obsidian/` config primes it in the list but does not install the binary. The README setup step will walk the buyer through this.
  </action>
  <verify>
    `ls -la bundle/obsidian-vault-starter/` shows: Daily/ Weekly/ Quarterly/ Goals/ Archive/ .obsidian/
    `ls bundle/obsidian-vault-starter/.obsidian/` shows: community-plugins.json, appearance.json
    `ls bundle/obsidian-vault-starter/Daily/` shows: _template-daily.md, example-2026-03-01.md
    `cat bundle/obsidian-vault-starter/.obsidian/community-plugins.json` shows obsidian-local-rest-api in plugins array
    No personal data in example notes: `grep -r "jellespek\|jelle\|Jelle\|lutjebroeker" bundle/obsidian-vault-starter/` returns nothing
  </verify>
  <done>
    Vault starter has all 5 top-level folders, .obsidian/ config, template files for each note type, and one realistic example note per type. All example notes use fictional placeholders. Local REST API is pre-configured in community-plugins.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write README setup guide and create final bundle ZIP</name>
  <files>
    bundle/README.md,
    bundle/execution-engine-am-bundle.zip
  </files>
  <action>
    Read `bundle/audit-notes.md` before writing. You need: workflow list (Section 5), Ollama model names (Section 3), Postgres table schema (Section 4), any community node requirements (Section 1).

    Write `bundle/README.md` following the locked structure from CONTEXT.md (see interfaces block). Specifics:

    **Header:**
    ```
    # Execution Engine AM Bundle — Setup Guide
    **"From download to first Telegram check-in in 30 minutes."**
    ```

    **Prerequisites Checklist:**
    - n8n **1.x or later** (check your version at Settings → About in your n8n instance — link: https://docs.n8n.io/hosting/installation/)
    - Telegram account (any device)
    - Obsidian (link: https://obsidian.md/download)
    - Ollama (link: https://ollama.ai) — include the exact model pull command from audit-notes.md e.g. `ollama pull llama3`
    - PostgreSQL (link: https://www.postgresql.org/download/)
    - If community nodes found in audit: "n8n community node: [name] — install via Settings → Community Nodes → Install → [npm-package-name]"

    **Step 1 — Telegram Bot:** Numbered steps for BotFather: open BotFather, /newbot, choose name, choose username, copy the token. Set commands: /start, /checkin, /plan (or whatever commands the actual workflows use — read from audit-notes if available, otherwise use these as standard). Verification: "Send /start to your bot. It should reply with a greeting message."

    **Step 2 — Obsidian Vault:**
    1. Extract `obsidian-vault-starter/` to a folder you will remember (e.g. `~/Documents/ExecutionEngine/`)
    2. Open Obsidian → Open folder as vault → select that folder
    3. Note the full path to this folder — you will need it in Step 7
    Verification: "Obsidian opens and you see the Daily, Weekly, Quarterly, Goals, Archive folders in the left sidebar."

    **Step 3 — Obsidian Local REST API Plugin:**
    1. In Obsidian: Settings → Community plugins → Turn off Safe mode → Browse → Search "Local REST API" → Install → Enable
    2. Settings → Local REST API → Generate API key → copy and store it
    3. Note the port (default: 27123) and your API key — both needed in Step 7
    Verification: "Settings → Local REST API shows 'Running on port 27123'. Send a test request: `curl http://localhost:27123/` — it should return a JSON response."

    **Step 4 — Ollama:**
    1. Install Ollama from https://ollama.ai
    2. Pull the required model(s): use exact command from audit-notes.md (e.g. `ollama pull llama3`)
    3. Ollama starts automatically. Default endpoint: http://localhost:11434
    Verification: "`curl http://localhost:11434/api/tags` returns JSON with your model listed."

    **Step 5 — PostgreSQL:**
    1. Install PostgreSQL, create a database (e.g. `n8n_memory`)
    2. Run these CREATE TABLE statements in psql or your preferred client: [INSERT EXACT SQL FROM audit-notes.md Section 4]
    3. Note your connection details: host, port, database name, username, password
    Verification: "Connect to the database and run `\dt` — your table(s) appear in the list."

    **Step 6 — Import Workflows:**
    1. Open your n8n instance
    2. Workflows → Import from file → select `workflows/am-workflows-v1.json`
    3. All workflows import in inactive state
    4. For each workflow that uses Postgres: open the workflow → Postgres node → Credentials → Create New → fill in your connection details from Step 5
    5. For each workflow that uses Telegram: open workflow → Telegram node → Credentials → Create New → paste bot token from Step 1
    6. For each workflow that uses Claude API: open workflow → Anthropic node → Credentials → Create New → paste API key
    Note about community nodes: if any were flagged in prerequisites, install them before importing.
    Verification: "All workflows appear in your workflow list. No node shows 'Unknown node type'."

    **Step 7 — Configure the Config Node:**
    1. Open each workflow one by one
    2. Click the "CONFIG — Edit These Values" node (first node after the trigger)
    3. Fill in the values you collected in steps 1-6:
       - telegramChatId: your Telegram chat ID (get it by sending a message to @userinfobot)
       - telegramBotToken: your bot token from Step 1
       - obsidianBaseUrl: http://localhost:27123 (or your actual port)
       - obsidianApiKey: your API key from Step 3
       - vaultRootPath: full path to your vault from Step 2 (e.g. /Users/you/Documents/ExecutionEngine)
       - postgresHost/Port/Db/User/Password: from Step 5
       - ollamaBaseUrl: http://localhost:11434
       - claudeApiKey: your Anthropic API key (https://console.anthropic.com)
    4. Activate the workflow using the toggle at the top right
    5. Repeat for each workflow
    Verification: "Activate the Morning Routine workflow and click 'Test workflow'. It runs to completion without red error nodes."

    **Workflow Reference section:** Use the workflow list from audit-notes.md Section 5, grouped exactly by: Morning Routine / Evening Routine / Week Planning / Quarter Review / Message Hub.

    **Support section:**
    ```
    ## Support
    Email: support@jellespek.nl
    Response time: within 24 hours
    ```

    After README is written, create the bundle ZIP:
    ```bash
    cd bundle && zip -r execution-engine-am-bundle.zip README.md workflows/ obsidian-vault-starter/
    ```

    Verify the ZIP before finalising — unzip listing should show all three top-level items.
  </action>
  <verify>
    1. README exists and is complete: `wc -l bundle/README.md` shows 200+ lines
    2. All 7 setup steps present: `grep -c "^## Step" bundle/README.md` returns 7
    3. Workflow reference section present: `grep -c "^### Morning Routine" bundle/README.md` returns 1
    4. Support section present: `grep "support@jellespek.nl" bundle/README.md`
    5. ZIP created: `ls -lh bundle/execution-engine-am-bundle.zip`
    6. ZIP contents correct: `unzip -l bundle/execution-engine-am-bundle.zip | grep -E "README|workflows|obsidian-vault-starter"`
  </verify>
  <done>
    README.md is a complete setup guide following the locked structure: prerequisites checklist, 7 numbered setup steps each with a verification step, workflow reference grouped by routine, support section. Bundle ZIP contains README.md, workflows/am-workflows-v1.json, and obsidian-vault-starter/ tree. A complete beginner can follow it to reach their first Telegram check-in.
  </done>
</task>

</tasks>

<verification>
- `bundle/README.md` has all 7 setup steps, each ending with a specific verification command/action
- Vault starter has no personal data (grep for jellespek, jelle, lutjebroeker in vault files)
- ZIP unpacks to README.md + workflows/ + obsidian-vault-starter/
- Workflow reference section in README matches audit-notes.md workflow list groupings
- Ollama model in README matches exact model name from audit-notes.md
- Postgres CREATE TABLE statements in README match audit-notes.md schema
</verification>

<success_criteria>
- README is complete, beginner-readable, and production-ready
- Vault Starter is complete with correct folder structure and .obsidian/ config
- Final ZIP is uploadable to Gumroad and contains all three deliverables
- No personal/private data in any file
</success_criteria>

<output>
After completion, create `.planning/phases/02-am-bundle-packaging/02-02-SUMMARY.md` with:
- README word count and section count
- Vault starter file list
- ZIP file path and size
- Any deviations from the planned structure (e.g. different folder names from audit)
- Ollama model(s) documented
- Postgres table name(s) documented
</output>
